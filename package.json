{
  "name": "xyops-kubernetes-management",
  "version": "1.2.0",
  "private": true,
  "description": "xyOps Kubernetes API management, diagnostics and controlled cleanup workflows",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "xyops-kubernetes-management": "runner.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check hygiene.js && node --check hardening.js && node --check pagination.js && node --check rollout.js && node --check token.js && node --check token-runner.js && node --check runner.js && node --check scripts/acceptance-smoke.js && node --check scripts/normalize-release.js",
    "acceptance:smoke": "node scripts/acceptance-smoke.js",
    "release:check": "node scripts/normalize-release.js",
    "release:normalize": "node scripts/normalize-release.js --write"
  },
  "engines": {
    "node": ">=18"
  }
}
